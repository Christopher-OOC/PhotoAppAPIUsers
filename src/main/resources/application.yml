server:
  port: 0
spring:
  application:
    name: PhotoAppAPIUsers
#  cloud:
#    gateway:
#      discovery:
#        locator:
#          enabled: true
  devtools:
    restart:
      enabled: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8010/eureka